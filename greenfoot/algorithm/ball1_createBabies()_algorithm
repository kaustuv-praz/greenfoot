/* algorithm for ball1.class createBabies() method starts*/

step 1: start.
step 2: check if "counter_baby" value is '0'.
            If true: create 2 new baby1 objects and add them on left side.
                     create 2 new baby2 objects and add them on right side.
            If false: Goto step 3.

step 3: increase the value of "counter_baby" by 1.
step 4: stop.

/* algorithm for ball1.class createBabies() method ends*/
